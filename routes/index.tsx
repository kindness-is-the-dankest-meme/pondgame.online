import { Game } from "../islands/Game.tsx";

/**
 * create a pond
 * fixed size of TV
 * crossfade / blur / contrast a mask
 * timer or click to regenerate
 * mousewheel or slider for scale
 * controls?
 * text?
 */
const IndexPage = () => (
  <main>
    <Game />
  </main>
);

export default IndexPage;
